import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    height: "50vh",
  },
  text: {
    fontSize: "40px",
    color: "#ffffff",
    letterSpacing: "2px",
  },
}));
