import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  receivedBubble: {
    backgroundColor: "slateblue",
    color: "white",
  },
  sentBubble: {
    backgroundColor: "#8E7C79",
    color: "white",
  },
}));
