import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  mainContainer: {
    marginTop: "2%",
  },
  likeText: {
    fontWeight: "600",
    fontSize: "1.2rem",
  },
  likeGrid: {
    position: "fixed",
  },
}));
